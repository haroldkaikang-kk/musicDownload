# 來源與版本

## 專案基礎

- 原始圖形介面：<https://github.com/MrsEWE44/musicDownload>
- 搜尋與下載核心：<https://github.com/CharlesPikachu/musicdl>
- musicdl 2.13.4 發行頁：<https://pypi.org/project/musicdl/2.13.4/>

## 本包固定版本

- MusicDownload Professional：`1.2.1`
- Python：`3.12.10`
- PySide6：`6.9.3`
- musicdl：`2.13.4`
- 目標：Intel `x86_64`、macOS `12.7.6`

## 音源狀態的判定方式

5sing、酷我、咪咕與千千音樂目前仍有官方網站或 App 服務，musicdl 2.13.4 也仍登記相關搜尋／下載模組。不過「平台仍營運」與「非官方接口此刻可用」是兩回事，因此 1.2.1 不把任何來源永久標成可用，而是加入即時「檢測音源」功能。

## 1.2.1 修正

修正 macOS 將 `/var` 映射到 `/private/var` 時，專業功能自檢對同一目錄產生錯誤判斷的問題；同時支援自動復用同層 1.2.0 資料夾內已建立的 Python 3.12 環境。

檢測結果分為：

- `正常`：在限制時間內取得至少一筆測試結果。
- `無結果`：請求完成，但測試關鍵字沒有可用結果。
- `失敗`：來源模組或網路請求發生錯誤。
- `超時`：超過設定時間，該獨立工作程序已被終止。

每個來源使用獨立程序，因此某一站失效、卡住或崩潰時，不會讓其他來源一起卡死。

## macOS 12 相容處理

PySide6 固定為 6.9.3。musicdl 的 `nodejs-wheel` 附件在這台 Intel macOS 12 上需要從原始碼編譯且會失敗；它主要服務本介面沒有提供的 YouTube 輔助路徑。因此製作器以 `--no-deps` 安裝 musicdl 2.13.4，並明確安裝此介面實際使用的其餘相依套件，排除 `nodejs-wheel`。

程式內不會自動升級 musicdl、PySide6 或其他核心相依套件；版本只在重新執行正式版製作器時依本檔與 `requirements.txt` 建立。

## HTTPS 證書原則

製作器先檢查 Python 到 PyPI 的 HTTPS 證書鏈，失敗時可執行 Python 3.12 官方的 `Install Certificates.command`。它不會強制關閉 VPN，不會停用 SSL 驗證，也不使用不安全的 `trusted-host` 參數。

## 建置方式

正式版由使用者自己的 Intel Mac 透過 PyInstaller 建立普通 `.app` 資料夾結構，再進行本機臨時簽署並由 `hdiutil` 生成 DMG。沒有改成啟動時必須解壓的單檔 App。
