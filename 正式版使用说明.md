# MusicDownload 1.2.1 Professional 使用說明

適用設備：**Intel Mac（x86_64）＋ macOS 12.7.6**  
建置用 Python：**Python 3.12.10**（可與你現有的 Python 3.14.2 並存）

## 這一版已完整加入

- 啟動時不預設勾選任何音源；仍可手動套用「國內主流、原創與獨立、海外流媒體、全部 17 個來源」預設。只有你在專業設定中主動開啟「音源記憶」後，才會恢復上次的手動選擇。
- 「檢測音源」按鈕，逐來源顯示正常、無結果、失敗或超時；單一來源失效不拖累其他來源。
- 搜尋與下載的硬超時、取消任務，以及安全終止相關子程序。
- 可調整順序的下載佇列；可暫停、繼續、取消全部。
- 逐首成功／失敗明細、失敗原因，以及「只重試失敗項目」。
- 下載後完整解碼檢查，顯示實際容器格式、編碼、取樣率、位深、位元率、聲道、時長與檔案狀態。
- 音質篩選、歌手／專輯／來源二次過濾，以及格式、取樣率、位元率、大小、來源排序。
- 自訂命名與資料夾模板。
- SQLite 下載歷史、重複歌曲提示，以及開啟原檔、重新下載、保留兩份或略過。
- 在 Finder 顯示、手動試聽、淺色／深色主題、恢復預設設定。
- 代理／VPN 診斷提示；程式不會關閉、切換或修改你的網路設定。
- 日誌自動輪替：每個檔案最多 2 MB，保留 3 份備份。

## 一、製作正式版

1. 解壓整個工程包；不要只把其中一個檔案單獨移出。
2. 按住 Control 點擊 `制作正式版.command`，選擇「打開」。
3. 等待八個步驟完成。第一次需要下載相依套件，時間取決於網路。
4. 完成後會自動打開 `release` 資料夾。

製作器只尋找 Python 3.12，不會移除或取代你的 Python 3.14.2。日常打開 `.app` 時也不需要先「打開 Python 3.12」。先前提到的「應用程式 → Python 3.12」，只是指該資料夾內的 `Install Certificates.command` 證書修復工具。

### 如果再次出現 CERTIFICATE_VERIFY_FAILED

這代表 Python 無法驗證 PyPI 的 HTTPS 證書，**不代表非得關閉 VPN**：

1. 到「應用程式 → Python 3.12」，執行 `Install Certificates.command`。
2. 看到 `update complete` 後，回到製作器重試。
3. 若仍失敗，可自行暫時切換 VPN／代理線路做對照測試；這只是診斷選擇。

製作器不會停用 SSL 驗證，也不會使用 `trusted-host` 繞過證書安全檢查。

## 二、安裝與打開

完成後雙擊：

```text
release/MusicDownload-1.2.1-macOS12-Intel.dmg
```

把 `MusicDownload.app` 拖入 `Applications`。由於它是在你的 Mac 本機臨時簽署、沒有 Apple 公證，第一次若被阻擋，請按住 Control 點擊 App，選擇「打開」。之後日常使用只需從「應用程式」打開 MusicDownload，不需要開終端或 Python。

### 1.2.1 建置修正

1.2.0 的兩條自檢把 macOS 同一個臨時目錄的 `/var/...` 與 `/private/var/...` 誤認為不同位置，會在程式核心其實正常時中止建置。1.2.1 已改為比較解析後的實際路徑，並增加路徑別名回歸測試。

若 1.2.0 資料夾仍在同一層，1.2.1 製作器會自動復用其中已安裝完成的 Python 3.12 `.venv`，不會重新下載全部相依套件。

## 三、建議的第一次操作

1. 啟動時所有音源都是未勾選狀態。
2. 先勾選「酷我音樂」，點「檢測音源」。
3. 檢測正常後，搜尋一首歌，單源數量先設為 2。
4. 在結果表點選一行，可試聽；勾選歌曲後加入下載佇列。
5. 在佇列中調整順序，點「開始下載」。
6. 完成後查看「下載與音訊檢查報告」。

「檢測音源」驗證的是當下的搜尋接口及測試關鍵字；它不能保證每一首歌曲都一定有可下載連結。音源網站日後改版時，狀態也可能改變。

## 四、音訊參數怎麼看

- 「格式」是檔案容器／副檔名，例如 FLAC、WAV、M4A。
- 「編碼」來自實際音訊流，例如 FLAC、ALAC、AAC、MP3、PCM S16LE。
- 「位深」只在 FLAC、ALAC、WAV／PCM 等能可靠判定的檔案顯示。
- MP3、AAC 不會被誤標一個虛假的 PCM 位深。
- 「解碼正常」代表 PyAV 已從頭到尾讀完音訊流，而不只是檢查副檔名。

下載後可從「文件 → 下載歷史與音訊參數」再次查看所有紀錄。

## 五、命名、重複與安全

可用模板欄位：

```text
{artist} {title} {album} {source} {format}
{sample_rate} {bit_depth} {bitrate}
```

模板內可用 `/` 建立子資料夾，例如 `{artist}/{album}/{title}`。非法檔名字元和越界路徑會被安全處理。

若選擇「重新下載」，舊檔會先移到 macOS 廢紙簍，不會直接永久刪除。選擇「恢復預設設定」只重設介面和來源設定，不會刪除歷史資料庫或音樂檔案。

## 六、VPN／代理提示

使用 VPN 並不是錯誤，也不是所有來源都要求關閉。程式只會：

- 偵測常見系統代理、VPN 連線與代理環境變數；
- 對尚未檢測的國內來源顯示橙色提醒；
- 讓你用「檢測音源」取得實際結果。

它不會擅自關閉 VPN、修改代理或替你改路由。只有在某來源實際失敗時，才值得切換線路做 A／B 測試。

## 七、日誌與疑難排查

應用日誌通常位於：

```text
~/Library/Logs/MusicDownload/musicdownload.log
```

可從「文件 → 打開日誌資料夾」直接查看。若要區分 App 打包問題與程式問題，可雙擊 `运行源码版.command`；它使用正式版建立的同一個 `.venv`，並保留終端輸出。

核心自檢可在終端執行：

```bash
./.venv/bin/python tests/test_core.py
```

## 八、版本邊界

此包的建置設定專門面向 Intel x86_64、macOS 12.7.6。換成 Apple Silicon 或升級到需要不同 Qt／Python 組合的系統時，應重新製作對應架構的版本。
