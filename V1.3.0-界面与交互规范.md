# MusicDownload V1.3.0 界面与交互规范

## 1. 产品视觉定位

V1.3.0 采用接近现代版 QQ 音乐、Apple Music、网易云音乐客户端的设计语言：柔和、轻盈、现代、有音乐感，并通过封面、文字层级和留白建立信息秩序。界面面向日常音乐搜索、播放和下载，同时给专业音频参数保留独立的高密度视图。

禁止采用以下方向：幼稚或卡通化视觉、Pro Tools 式冷硬密集工具风、刺眼纯白、粗硬边框、霓虹色、廉价渐变、满屏彩色按钮、把所有控件做成胶囊形，以及用 Emoji 充当正式界面图标。

正式图标优先使用 macOS 可识别的系统符号或仓库内统一风格的单色矢量资源。图标必须有中文工具提示和可访问名称，不能只靠颜色表达状态。

## 2. 视觉令牌

### 2.1 默认柔和浅色

| 用途 | 建议值 | 约束 |
|---|---|---|
| 主背景 | `#F5F6F3` 暖灰白 | 不使用 `#FFFFFF` 作为整窗背景 |
| 内容面板 | `#FCFDFC` | 仅用于卡片、列表和输入区域 |
| 左侧栏 | `#EAF1EE` 淡灰绿；蓝色主题可用 `#EAF0F4` | 与内容区有层次但不形成强色块 |
| 主文字 | `#202724` | 正文对比度不低于 WCAG AA |
| 次文字 | `#66706C` | 不用于关键错误 |
| 分隔线 | `#DCE3DF` | 1 px 细线 |
| 默认强调色 | `#3F8F7C` 低饱和青绿 | 搜索、加入队列、开始下载等主操作 |
| 悬停强调色 | `#347866` | 只在可点击控件使用 |
| 选中背景 | `#DDEDE7` | 文字仍保持清晰 |
| 成功 | `#3C8C62` | 只表达正常或成功，不作普通按钮色 |
| 警告 | `#B07932` | 用于无结果、需要注意，不铺满主界面 |
| 错误/危险 | `#C44C4C` | 只用于失败和真正危险操作 |

### 2.2 柔和深色

| 用途 | 建议值 | 约束 |
|---|---|---|
| 主背景 | `#171B19` | 避免纯黑 |
| 内容面板 | `#202522` | 面板层级清楚 |
| 左侧栏 | `#1C2925` | 不使用高饱和色块 |
| 主文字 | `#E7ECE9` | 正文对比度不低于 WCAG AA |
| 次文字 | `#A7B1AC` | 禁用态仍可辨认 |
| 分隔线 | `#343C38` | 1 px |
| 默认强调色 | `#67B39F` | 不使用发光/霓虹效果 |

### 2.3 形状、间距与层次

- 圆角范围 8–12 px：输入框和小按钮 8 px，面板和列表卡片 10–12 px。
- 主窗口外边距 16 px；主要分区间距 12–16 px；同组控件间距 8 px。
- 行高随列表密度变化：紧凑 44 px、标准 56 px、宽松 68 px。
- 阴影只用于悬浮菜单、弹层和拖动中的卡片；普通面板使用细分隔线或极轻阴影，不叠加两者。
- 普通按钮使用圆角矩形，不默认使用全胶囊形；状态标签和短音质标签可以使用小胶囊。
- 动画时长 120–200 ms；不得用持续弹跳、呼吸或大幅位移动画。

## 3. 主界面信息架构

主窗口至少包含左侧音源侧边栏、顶部搜索区、搜索结果页签、下载队列页签、下载历史页签、现代列表/专业表格切换、底部固定播放器和偏好设置入口。

```text
┌──────────────────────────────────────────────────────────────────────────────┐
│ MusicDownload                         [偏好设置] [最小化/最大化/关闭]        │
├──────────────────────┬───────────────────────────────────────────────────────┤
│ 音源                 │  [搜索歌曲⌄] [ 输入歌曲、歌手或专辑……        ] [搜索] │
│                      │                                       [取消搜索]      │
│ 音源预设 [应用]      ├───────────────────────────────────────────────────────┤
│ [全选] [清空]        │  [搜索结果] [下载队列] [下载历史]                     │
│ [检测所选] [检测全部]│                                                       │
│                      │  搜索结果工具栏：                                     │
│ ○ 苹果音乐           │  [现代列表｜专业表格] [音质筛选⌄] [来源筛选⌄] [排序⌄]│
│ ○ Deezer             │  [全选] [取消全选]               [加入下载队列（3）] │
│ ○ 5sing              │  ───────────────────────────────────────────────────  │
│ ○ Jamendo            │                                                       │
│ ○ Joox               │  封面  歌曲/歌手/专辑  格式/音质  来源   播放 下载 队列│
│ ○ 酷我音乐           │  封面  歌曲/歌手/专辑  格式/音质  来源   播放 下载 队列│
│ ○ ……（17 项可滚动）  │                                                       │
│                      │  空白时：图标 + 尚未搜索音乐 + 操作指引               │
│ 下载历史             │                                                       │
│ 偏好设置             │                                                       │
├──────────────────────┴───────────────────────────────────────────────────────┤
│ [封面]  当前歌曲名 / 歌手    [上一状态不保留] [播放/暂停] [停止]             │
│         00:42 ───────────────●──────────────────────── 03:58   [静音][音量] │
└──────────────────────────────────────────────────────────────────────────────┘
```

布局约束：

- 左侧栏建议宽 220–260 px，可滚动但不横向滚动；17 个来源直接列出，不用“更多来源”隐藏常用项。
- 顶部搜索框是主内容区视觉中心，搜索模式在其左侧，搜索与取消在右侧。
- 保存目录、命名规则、搜索数量、网络超时等不与搜索框挤在同一横排；它们进入相应偏好设置，主界面只在队列/下载区域显示实际保存路径摘要。
- 主内容区页签固定为“搜索结果”“下载队列”“下载历史”；切页不能停止播放、搜索或下载。
- 底部播放器固定高度 76–92 px；窗口缩小时先压缩歌曲信息和进度区域，不隐藏播放、停止、静音等核心操作。

## 4. 左侧音源侧边栏

### 4.1 结构

1. 顶部为“音源”标题和折叠/展开能力；普通桌面宽度默认展开。
2. “音源预设”下拉框与“应用”并列。
3. “全选”“清空”使用次要描边样式。
4. “检测所选”“检测全部”分开；检测所选在未选来源时禁用，不再用确认框把它隐式变成检测全部。
5. 17 个来源按“国内主流”“原创与独立”“海外流媒体”等清晰分组，以可滚动列表显示；分组不得减少或重复来源，每项包含复选框、名称和小型状态点。
6. 底部固定“下载历史”和“偏好设置”入口；它们分别切到历史页签和打开偏好设置。

### 4.2 状态点

| 状态 | 颜色/形态 | 辅助文本 |
|---|---|---|
| 未检测 | 中性灰空心点 | “尚未检测” |
| 检测中 | 强调色旋转小环 | “检测中” |
| 正常 | 绿色实心点 | “正常，返回 n 条，耗时 x.x 秒” |
| 无结果 | 琥珀色实心点 | “请求完成但无结果” |
| 超时 | 红色空心感叹点 | “超过 n 秒，已终止” |
| 失败 | 红色实心点 | 显示可理解的错误摘要 |

状态详情通过悬停工具提示、键盘聚焦提示和检测结果详情区提供。主界面不得因代理/VPN 检测把整段来源文字改为橙色；网络诊断信息进入“偏好设置 → 网络”或非阻塞状态提示。

## 5. 顶部搜索区

- 搜索模式选项固定为“搜索歌曲”和“解析歌单链接”。
- “搜索歌曲”占位文案：“输入歌曲、歌手或专辑”。
- “解析歌单链接”占位文案：“粘贴支持的歌单链接”。
- `Return` 执行搜索；输入法候选确认期间不得误触发。
- 搜索运行时：“搜索”禁用并显示进行状态，“取消搜索”启用；取消后保留已返回结果并明确标注任务已取消。
- 没有选择音源时点击搜索，在搜索框下方或音源侧边栏显示：“请先选择至少一个音乐来源。”不得自动替用户选择来源。
- 输入为空时显示：“请输入歌曲、歌手、专辑或歌单链接。”焦点回到搜索框。
- 搜索失败按来源记录，不用单一模态弹窗阻断仍在运行或已成功的来源。

## 6. 搜索结果、空白和加载状态

### 6.1 空白状态

未搜索时，主内容区显示柔和单色图标、标题“尚未搜索音乐”和说明“选择左侧音源，输入歌曲、歌手或专辑”。不显示大片无说明的空表格。

搜索完成但无结果时显示“没有找到可用结果”，并提供“查看音源状态”“修改关键词”两个次要入口；不得把“无结果”写成“搜索失败”。

### 6.2 现代列表模式

每项从左到右显示：选择框、封面、歌曲名、歌手、专辑、格式、音质标签、来源，以及播放、立即下载、加入队列三个操作。歌曲名是第一信息层，歌手与专辑是第二层；格式、音质和来源使用低干扰短标签。操作图标默认单色，悬停时显示中文工具提示。

### 6.3 专业表格模式

列顺序固定为：选择、封面、歌曲名、歌手、专辑、格式、编码、采样率、位深、比特率、声道、文件大小、时长、来源。表头可点击排序，但工具栏“排序”仍是主要、可发现的排序入口。数值未知时显示“未知”或“—”，不得显示 `0kHz`、`0bit` 等伪值。

### 6.4 选择、筛选与视图切换

- 两种视图共享同一结果模型和稳定歌曲标识；排序、筛选、异步封面返回后不得错绑歌曲。
- “全选”“取消全选”只作用于当前可见结果；隐藏结果的既有选择保持不变。
- 主按钮文案实时显示“加入下载队列（n）”；`n=0` 时禁用。
- 不存在“下载范围”下拉框，也不存在下载“未勾选”项目的入口。
- 音质筛选和排序是两个独立控件。来源筛选根据实际结果动态生成；结果只有一个实际来源时隐藏，若布局需要保留则禁用并显示该来源名，不能只剩“全部来源”。

## 7. 按钮层级与反馈

| 层级 | 控件 | 样式 | 规则 |
|---|---|---|---|
| 主操作 | 搜索、加入下载队列、开始下载 | 强调色实底 | 同一区域通常只保留一个最主要实底按钮 |
| 次要操作 | 浏览、筛选、检测音源、打开文件夹、全选、取消全选 | 中性色或描边 | 不与主操作争夺视觉中心 |
| 播放操作 | 播放/暂停 | 强调色图标按钮 | 底部播放器可使用圆形，但不扩散到全部按钮 |
| 危险操作 | 取消全部、清除记录、替换文件 | 红色只用于文字、图标或确认态 | 必须有明确对象和后果；普通取消对话框不使用红色 |
| 状态 | 正常、成功 | 绿色状态点/标签 | 绿色不作为普通操作按钮背景 |

点击反馈：即时改变按下态；预计超过 300 ms 的操作显示进行状态；成功操作尽量用非阻塞状态条/轻提示，失败信息靠近触发位置并可查看详情。不可恢复操作必须确认，可恢复操作说明恢复位置，例如“旧文件将移到废纸篓”。

## 8. 底部固定播放器

播放器固定显示封面、歌曲名、歌手、播放/暂停、停止、当前时间、总时长、可拖动进度条、音量和静音。

- 未选择歌曲：播放键禁用，显示“选择一首歌曲开始播放”，时间为 `--:-- / --:--`。
- 选中但未播放：点击播放载入选中项；右键“播放”和双击结果直接载入并播放。
- 播放中：播放键变为暂停；点击暂停后图标恢复播放，点击继续从原位置恢复。
- 停止：音频停止并将进度归零；歌曲信息保留，便于再次播放。
- 播放错误：播放器信息区显示“播放失败”及简短原因；详情可展开，界面不锁死。
- 播放结束：进入停止状态，不选择或播放下一首，不循环。
- 切换主界面页签、筛选或排序不得中断当前播放，也不得把播放器错误绑定到已重用的结果行。

## 9. 下载队列页签

队列顶部显示实际保存路径和“打开下载文件夹”，中部表格列为歌曲、歌手、来源、状态、详细信息、进度，底部提供开始、暂停/继续、取消、上移、下移、重试失败项。

- 未开始：允许上移/下移，开始按钮在有等待项且路径有效时启用。
- 运行中：禁用排序调整和重复开始；暂停提示“当前歌曲完成后暂停”。
- 暂停：开始新项目被阻止，当前已完成的项目状态不回退。
- 取消：确认对象是当前队列；终止当前工作进程，剩余项标记“已取消”。
- 单项进度无法从上游取得时使用不确定进度动画，不得伪造百分比；总队列进度按终态项目数计算。
- 失败/超时/文件异常保留错误摘要和“重试失败项”；成功项不重复加入重试集合。

## 10. 下载历史页签

历史页提供“下载历史”和“搜索历史”两个区域或子页。

下载历史显示歌曲、歌手、来源、日期、状态、音频参数、文件路径，并提供“在访达中显示”“清理失效记录”。音频参数详情包含格式、编码、采样率、位深、比特率、声道、时长和完整解码状态。

搜索历史至少记录搜索词、搜索模式、所选来源、时间、结果数和各来源状态摘要；不得保存认证令牌、cookie 或完整请求头。清理失效记录只删除指向已不存在文件的下载历史行，不删除现存音乐文件。

## 11. 偏好设置

通过菜单“MusicDownload → 偏好设置…”、左侧栏“偏好设置”或 `Command + ,` 打开。偏好设置使用正式分栏或页签，不采用单一拥挤表单。

### 11.1 分页

1. 外观
2. 一般
3. 音源
4. 下载
5. 音频检查
6. 网络
7. 历史与日志

### 11.2 外观

- 主题：跟随系统、柔和浅色、柔和深色。
- 字体：系统字体、苹方、电脑已安装字体。若选择的字体后续不可用，回退系统字体并提示一次。
- 字号：11–18 pt。
- 字重：常规、中等、半粗。
- 界面缩放：90%–140%。
- 列表密度：紧凑、标准、宽松。
- 封面大小：小、中、大。
- 强调色：青绿、蓝色、紫色、珊瑚红；都使用低饱和值，错误/成功状态色不跟随强调色。
- 即时预览：只作用于偏好设置预览区和当前窗口视觉，不写入底层业务状态；点击“取消”恢复打开前外观。
- “恢复外观默认值”只重置外观，不清除来源、路径、历史和队列。

### 11.3 其他分页边界

- 一般：搜索后自动处理三态、启动/退出通用行为、默认结果视图。
- 音源：每来源数量、来源记忆、预设管理和检测说明。
- 下载：保存目录、命名模板与预览、重复文件策略。
- 音频检查：完整解码开关只有在底层确实支持可配置时才出现；默认不得降低当前完整检查等级。
- 网络：搜索硬超时、下载硬超时、并行来源数、只读网络诊断信息；不得提供会暗中修改 VPN/代理的开关。
- 历史与日志：历史存储位置、日志目录、打开目录、清理策略和日志级别（只有底层实现后才显示）。
- 没有底层功能的选项不得先做成无效开关；未实现项不进入正式 App 控件树。

## 12. 主要控件交互验收表

| 控件 | 所在位置 | 默认状态 | 可用条件 | 点击行为 | 禁用状态 | 键盘操作 | 失败提示 |
|---|---|---|---|---|---|---|---|
| 音源复选框 | 左侧音源列表 | 全不选 | 无同来源状态写冲突 | 切换该来源选择 | 搜索已提交后仍可改下一次选择，但不改变当前任务 | `Space` 切换 | 无；状态不应静默回弹 |
| 音源状态点 | 来源名称右侧 | 未检测灰点 | 有/无检测均可聚焦 | 点击或展开显示最近详情 | 无 | `Return` 展开详情 | “尚未检测”或具体错误摘要 |
| 音源预设 | 左侧上方 | “选择预设…” | 无 | 选择后待“应用”确认 | 搜索进行中可允许为下一次准备，但不得改当前任务 | 方向键选择，`Return` 确认 | 预设无效时不改变选择 |
| 应用预设 | 音源预设右侧 | 次要按钮 | 选择有效预设 | 用预设替换当前来源选择 | 未选有效预设时禁用 | `Return`/`Space` | “请选择一个音源预设” |
| 全选来源 | 左侧上方 | 次要按钮 | 来源列表已加载 | 勾选 17 项 | 列表未加载时禁用 | `Return`/`Space` | 不使用弹窗 |
| 清空来源 | 左侧上方 | 次要按钮 | 至少一项已选 | 清空 17 项 | 0 项时禁用 | `Return`/`Space` | 不使用弹窗 |
| 检测所选 | 左侧上方 | 次要按钮 | 至少一项已选且无搜索/检测任务 | 创建所选来源检测任务 | 无选择或任务冲突时禁用 | `Return`/`Space` | “请先选择音源”或“请先结束当前搜索” |
| 检测全部 | 左侧上方 | 次要按钮 | 无搜索/检测任务 | 检测全部 17 项 | 任务运行时禁用 | `Return`/`Space` | 按来源显示失败，不用总失败弹窗 |
| 搜索模式 | 顶部搜索框左侧 | 搜索歌曲 | 无搜索运行；运行中保持只读当前模式 | 切换模式并更新占位文案 | 搜索运行中禁用 | 方向键，`Alt+Down` | 无 |
| 搜索输入框 | 顶部中心 | 空 | 无 | 输入关键词/链接 | 搜索运行时可保持可复制，但不得触发第二任务 | `Return` 搜索，`Escape` 清空或取消按上下文处理 | 输入为空时就地提示 |
| 搜索 | 顶部右侧 | 主按钮 | 有输入、至少一来源、无冲突任务 | 启动逐来源搜索 | 条件不满足或搜索中禁用 | `Return` | 明确缺输入/缺来源；来源错误分项显示 |
| 取消搜索 | 搜索按钮右侧 | 禁用 | 搜索运行 | 安全终止所有当前来源子进程，保留已返回结果 | 无任务时禁用 | `Escape` | 取消超时则提示仍在终止，不假报完成 |
| 主内容页签 | 主内容顶部 | 搜索结果 | App 已加载 | 切换结果/队列/历史 | 不禁用；任务运行不受切页影响 | `Control+Tab`/`Control+Shift+Tab` | 页加载失败显示就地错误 |
| 视图切换 | 结果工具栏 | 用户上次选择或现代列表 | 结果页可用 | 切换现代列表/专业表格 | 无 | 方向键/`Space` | 切换失败不得清空模型 |
| 音质筛选 | 结果工具栏 | 全部音质 | 有结果 | 只改变可见集合 | 无结果时禁用 | 方向键 | 解析不了的结果进入“未知” |
| 来源筛选 | 结果工具栏 | 全部来源 | 至少 2 个实际来源 | 按实际来源过滤 | 0–1 个实际来源时隐藏或禁用 | 方向键 | 无 |
| 排序 | 结果工具栏 | 默认顺序 | 有结果 | 改变当前可见结果顺序 | 无结果时禁用 | 方向键 | 未知数值稳定置后，不弹错 |
| 全选结果 | 结果工具栏 | 次要按钮 | 有至少一条可见未选结果 | 勾选当前可见结果 | 无可见结果或已全选时禁用 | `Command+A` 仅当焦点在结果区 | 无 |
| 取消全选结果 | 结果工具栏 | 次要按钮 | 当前可见结果有已选项 | 取消当前可见结果 | 无选中项时禁用 | `Command+Shift+A` | 无 |
| 结果复选框 | 每个结果项 | 未选 | 有结果 | 切换该歌曲选择并更新主按钮计数 | 项不可用时禁用并说明 | `Space` | 无 |
| 结果右键菜单 | 结果项 | 关闭 | 指针/键盘定位结果 | 显示播放、立即下载、加入队列、全选、取消全选 | 不适用项单独禁用 | `Shift+F10` 或菜单键 | 无可播放地址时禁用播放并给工具提示 |
| 播放结果 | 现代列表/右键/双击 | 次要图标 | 有在线地址或现存本地文件 | 载入并播放该歌曲 | 无可播放媒体时禁用或点击后解释 | `Return` 可执行当前默认动作；双击播放 | “播放失败：原因” |
| 立即下载 | 现代列表/右键 | 次要操作 | 保存路径有效、歌曲可下载 | 为该曲创建立即运行任务 | 已有冲突下载策略处理时按状态禁用 | `Return` | 具体到歌曲、来源和原因 |
| 加入单曲队列 | 现代列表/右键 | 次要操作 | 歌曲有效 | 将该曲加入等待队列 | 已在等待队列时可禁用并提示 | `Return` | “无法加入队列：原因” |
| 加入下载队列（n） | 结果工具栏右侧 | `n=0`、禁用 | n>0 且保存路径有效 | 加入当前可见已勾选项 | n=0 或路径无效时禁用 | `Return` | 显示路径验证或队列错误 |
| 开始下载 | 队列页 | 主按钮 | 有等待项、无其他活动下载 | 启动当前队列 | 运行中、空队列、路径无效时禁用 | `Return` | “已有下载队列正在运行”等具体提示 |
| 暂停/继续 | 队列页 | 暂停、禁用 | 队列运行 | 请求在当前歌曲完成后暂停；暂停后变继续 | 未运行时禁用 | `Space` | 不能暂停时说明当前状态 |
| 取消队列 | 队列页 | 危险操作、禁用 | 队列运行 | 确认后安全取消当前和剩余项 | 未运行时禁用 | `Escape` 不直接执行危险取消；需确认 | “正在安全取消…”；失败时说明未退出进程 |
| 上移/下移 | 队列页 | 次要按钮 | 队列未运行且选中有效项 | 改变执行顺序 | 运行中或边界位置禁用 | `Command+Up`/`Command+Down` | 无 |
| 重试失败项 | 队列页 | 次要按钮 | 有失败/超时/文件异常项且队列未运行 | 只重置失败类项目 | 无失败项时禁用 | `Return` | “没有可重试项目” |
| 打开下载文件夹 | 队列页/历史页 | 次要按钮 | 保存目录存在 | 在访达打开目录 | 路径无效时禁用 | `Return` | “文件夹不存在或无法打开” |
| 在访达中显示 | 历史项 | 次要操作 | 记录路径存在 | 在访达选中文件 | 文件不存在时禁用并标“失效” | `Return` | “文件已不存在，可清理记录” |
| 清理失效记录 | 历史页 | 危险程度低的确认操作 | 有失效记录 | 确认后只删失效数据库记录 | 无失效记录时禁用 | `Return` 后确认 | 显示删除数量；数据库错误保留原记录 |
| 播放/暂停 | 底部播放器 | 禁用 | 已选或已载入歌曲 | 播放、暂停、继续三态切换 | 无歌曲时禁用 | `Space` 仅当焦点不在输入框且产品快捷键已实现 | “播放失败：原因” |
| 停止播放 | 底部播放器 | 禁用 | 播放或暂停 | 停止并归零 | 已停止时禁用 | 可定义 `Command+.`，实现前不显示提示 | 无 |
| 播放进度条 | 底部播放器 | 0、禁用 | 媒体已加载 | 拖动 seek；点击轨道定位 | 不可 seek 或未加载时禁用 | 左右方向键微调 | “此媒体不支持定位” |
| 音量 | 底部播放器 | 70% | 音频输出可用 | 调整 0–100% | 音频设备不可用时禁用 | 上下/左右方向键 | “没有可用音频输出设备” |
| 静音 | 底部播放器 | 未静音 | 音频输出可用 | 静音/恢复原音量 | 音频设备不可用时禁用 | `M`，但输入框聚焦时不触发 | 同上 |
| 偏好设置 | 菜单/左侧栏 | 可用 | App 已启动 | 打开偏好设置并定位上次分页 | 模态冲突时暂缓打开 | `Command+,` | 设置读取失败时回退默认并提示 |
| 恢复外观默认值 | 偏好设置“外观” | 次要按钮 | 外观页 | 只恢复外观选项并即时预览 | 默认值已生效时可禁用 | `Return` | 不得清除下载/历史设置 |
| 保存偏好设置 | 偏好设置底部 | 主按钮 | 所有可见字段有效 | 原子保存并应用 | 任一字段无效时禁用或阻止 | `Command+S` 或 `Return` | 聚焦首个错误字段并解释 |
| 取消偏好设置 | 偏好设置底部 | 次要按钮 | 对话框打开 | 放弃未保存业务设置，恢复打开前即时预览外观 | 无 | `Escape` | 无 |

## 13. 键盘、可访问性与窗口缩放

- 所有图标按钮必须有中文可访问名称、工具提示和明显键盘焦点环。
- 颜色不是唯一状态信号；状态点同时提供形状、文本或辅助技术标签。
- Tab 顺序按视觉顺序：左侧来源区 → 顶部搜索 → 页签/工具栏 → 结果 → 底部播放器。
- `Command + ,` 必须打开偏好设置；快捷键只在底层动作已实现并经过冲突检查后显示。
- 90%–140% 界面缩放和 11–18 pt 字号下不得裁切按钮关键文字；专业表格允许横向滚动，主搜索区与播放器核心操作不得消失。
- VoiceOver 读取结果项时至少包含歌曲名、歌手、来源、选择状态和可用操作。

## 14. 界面发布验收门槛

1. 在柔和浅色、柔和深色、跟随系统三种主题下截图核对主界面、三个主页签、偏好设置七页、右键菜单和错误状态。
2. 在 90%、100%、140% 缩放，11 pt 与 18 pt 字号，紧凑/标准/宽松密度下检查裁切、重叠和横向滚动。
3. 用键盘完成选择音源、搜索、选择结果、播放、加入队列、开始/暂停/继续/取消下载、打开偏好设置和保存设置。
4. 验证界面中没有 Emoji 正式图标，没有用户可见“试听”，没有“下载范围/未勾选”，没有无底层功能的开关。
5. 验证播放、搜索、下载在主界面切页和筛选/排序后继续运行，且所有异步回调仍绑定正确歌曲。
