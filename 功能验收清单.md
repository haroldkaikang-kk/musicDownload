# MusicDownload 1.2.1 功能驗收清單

## 已自動驗證

- [x] `musicdownload.py`、`musicdownload_core.py`、PyInstaller spec 語法編譯通過。
- [x] 17 個介面音源名稱都存在於 musicdl 2.13.4 的已註冊模組中。
- [x] 真實 WAV 完整解碼通過，正確取得 44.1 kHz、16 bit、雙聲道與 PCM S16LE。
- [x] 未知／空白的取樣率和位元率不會讓結果排序崩潰。
- [x] 自訂資料夾模板會限制在使用者選定的保存目錄內。
- [x] macOS `/var` → `/private/var` 路徑別名回歸測試通過。
- [x] 歷史資料庫能記錄並找回實際音訊編碼及原檔。
- [x] Shell 製作器語法通過。
- [x] 來源搜尋與單曲下載都使用可硬終止的獨立程序。

## 在目標 Mac 建置時自動執行

- [ ] Python 3.12 與 PyPI HTTPS 證書檢查。
- [ ] PySide6、QtMultimedia、PyAV、Mutagen、musicdl 核心匯入檢查。
- [ ] `tests/test_core.py` 專業功能自檢。
- [ ] PyInstaller 產生 Intel x86_64 `.app`。
- [ ] 本機簽署與 `codesign --verify --deep --strict`。
- [ ] 產生 `MusicDownload-1.2.1-macOS12-Intel.dmg`。

## 建議人工驗收

1. 確認首次啟動沒有任何預設音源勾選。
2. 分別檢測 5sing、酷我、咪咕、千千音樂；保留每個來源的實際狀態。
3. 搜尋三個來源，故意把一個來源超時設短，確認其他來源結果仍保留。
4. 下載兩首歌曲，測試暫停、繼續、取消與只重試失敗項目。
5. 用 FLAC／WAV 與 MP3／AAC 各測一次，確認位深顯示規則正確。
6. 再次下載同一首歌，逐一測試打開原檔、略過、保留兩份、重新下載。
7. 測試 Finder 顯示、下載歷史、淺／深色主題及恢復預設設定。
